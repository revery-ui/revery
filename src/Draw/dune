(library
    (name Revery_Draw)
    (public_name Revery.Draw)
    (preprocess (pps lwt_ppx))
    (libraries skia skia.wrapped skia.wrapped.bindings brisk-reconciler integers lwt lwt.unix sdl2 flex rebez.lib Revery_Core Revery_Font Revery_Math))
