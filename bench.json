{
  "source": "./package.json",
  "scripts": {
      "run": "esy '@bench' x ReveryBench"
  },
  "override": {
      "build": ["dune build -p Revery,ReveryBench -j4"],
      "install": [
          "esy-installer ReveryBench.install"
      ],
    "dependencies": {
      "reperf": "^1.4.0"
    },
    "devDependencies": {
      "ocaml": ">=4.6.0 <4.9.0"
    }
  }
}
