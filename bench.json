{
  "source": "./package.json",
  "scripts": {
      "run": "esy '@bench' x ReveryBench"
  },
  "override": {
      "build": ["dune build --root . -j4"],
      "install": [
          "esy-installer ReveryBench.install"
      ]
  }
}
